import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function ClustersPage() {
  return (
    <PlaceholderPage
      title="Topic Clusters"
      description="Organize blog content into pillar-and-cluster topic groups for SEO."
      icon="🕸️"
      features={["Pillar articles", "Supporting content", "Internal linking", "Cluster health"]}
    />
  );
}
