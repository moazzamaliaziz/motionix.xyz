import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function KeywordsPage() {
  return (
    <PlaceholderPage
      title="Keywords"
      description="Track target keywords, search volume, difficulty scores, and ranking positions."
      icon="🔍"
      features={["Keyword tracking", "Volume data", "Difficulty scores", "Position history"]}
    />
  );
}
