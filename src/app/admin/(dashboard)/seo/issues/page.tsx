import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function SeoIssuesPage() {
  return (
    <PlaceholderPage
      title="SEO Issues"
      description="View and resolve technical SEO issues found during site audits."
      icon="⚠️"
      features={["Issue detection", "Severity ranking", "Resolution tracking", "Audit history"]}
    />
  );
}
